# Pinned to the versions the project is developed and tested against.
#
# These were `>=` ranges, so CI quietly installed pandas 3.0.6 while local
# development ran 2.2.2. The two majors disagree about missing values -- NaN
# vs NaT vs pd.NA, and string dtypes -- which is why three separate null bugs
# passed every local test and failed only in the nightly job. Upgrade these
# deliberately, with the test suite, never by drift.
pandas==2.2.2
numpy==1.26.4
duckdb==1.0.0
requests==2.32.3
yfinance==1.2.0
flask==3.0.3
pytest==8.3.2
